#include<stdio.h>
float calculatebill(char wardtype,int days)
{
    float amount;
    float price;
    switch(wardtype)
    {
        case'G':{
        price=1000.0f;    
        }break;
        case's':{
            price=2000.0f;
        }break;
        case'p':{
        price=5000.0f;
        }break;
    }
    amount=price*days_admitted;
        if(days>7)
        {
            return amount;
        }
        int main(){
    char name[255];
    char wardtype;
    int daysadmitted;
    printf("enter the patient name");
    scanf("%s",name);
    printf("enter the wardtype:");
    scanf("%c",&wardtype);
    printf("enter number of days:");
    scanf("%d",&daysadmitted);
    float amount= calculatebill(wardtype,daysadmitted);
    printf("patient name:%s",name);
     printf("bill amount:%.21",amount);
    return 0;
}
